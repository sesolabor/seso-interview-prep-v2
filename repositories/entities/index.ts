import User from "./user";
import UserPasswordResetToken from "./user-password-reset-token";
import Enterprise from "./enterprise";

const entities = {
  Enterprise,
  User,
  UserPasswordResetToken,
};

export default entities;
